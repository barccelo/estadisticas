{
  "name": "club-deportivo-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply club-deportivo-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply club-deportivo-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
